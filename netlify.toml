
[build]
  command = "npm run build"  # Ensure this matches your local build command
  functions = ""  # Remove or set to your serverless functions path if needed
  publish = "dist/client,dist/server"  # Tell Netlify to publish both folders
